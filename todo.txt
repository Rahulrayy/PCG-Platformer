
1. Intro screen to hide chunk generation lag
   - Display a title/loading screen while chunk 0 is being generated on startup
   - OR move generation off the main thread earlier so it's ready before the game starts
   - Harder alternative: run chunk generation on a separate core(looked into it difficult with arcade

    2. Make headless runner more smarter
   - Currently the bot just moves right and jumps at walls/gaps... gets stuck on complex wall sequences
   - A smarter bot would reduce reliance on A* being the sole physics validator
   - or just go thr rl route but that would require a chunks which we know will pass 100%

3. 1/30 chunks are still unplayable
   - A* passes chunks that have paths only an ideal agent can take (somehow i still see too tall jumps)..this depends on the ca hyperparameters too



My Fav CA hyperparameters


CHANCE_TO_START_ALIVE = 0.57 #
DEATH_LIMIT = 4
BIRTH_LIMIT = 5
NUMBER_OF_STEPS = 6



4. Add a json that updates with highscore and make 2 diff counter one with current score which resets and another with highscore


5. Have a give up button if player ends up in un recoverable spot